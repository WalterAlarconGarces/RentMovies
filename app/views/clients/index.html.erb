<p style="color: green"><%= notice %></p>
<div id="div_clients">
<h1>Clients</h1>
<%= form_with url: clients_path, method: :get do |form|%>
 <%= form.text_field :query_text, placeholder: 'Client, age or rented movie', value: params[:query_text]%>
 <button class="btn btn-outline-success" type="submit">Search</button>
</div>

<%end%>
<div id="clients">
  <div class="container text-center">
    <div class="row row-cols-4">
      <% @clients.each do |client| %>
        <%= render client %>
      <% end %>
    </div>
  </div>
</div>

<div class="container ">
<div class="row ">
<div class="col"></div>
  <div class="col">
    <p><%=raw pagy_nav(@pagy)%></p>
  </div>
  <div class="col"></div>
</div>
</div>
